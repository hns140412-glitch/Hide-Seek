# Hide & Seek Runtime V2

This directory is the rewrite target created under TAKY TKY-PRODUCT-001.

The existing V1 application remains a reference implementation and regression source. V1 code/tests/contracts are reusable assets only; they do not count as V2 completion until migrated into the V2 runtime and revalidated in the active user path.

Current V2 implementation status: scaffold only.
